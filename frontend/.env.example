# Same-origin default. In local dev, Vite proxies /api to the backend.
# In production, Nginx serves the frontend and proxies /api to FastAPI.
VITE_API_BASE_URL=/api
VITE_DEVELOPER_ACCESS_SLUG=vh-control-9f4c2a
