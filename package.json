{
  "name": "visahouselms-root",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"cd backend && source .venv/bin/activate && uvicorn app.main:app --reload\" \"cd frontend && npm run dev\"",
    "dev": "npm run start",
    "seed": "cd backend && .venv/bin/python scripts/seed_local_data.py"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
